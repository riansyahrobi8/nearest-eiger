<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MyOverflowButtonStyle" parent="AppTheme">
        <item name="android:textColorSecondary">@color/textColorWhite</item>
    </style>
    <style name="Theme.EigerTerdekat.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.EigerTerdekat.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>